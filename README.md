# منصة رابِط لإدارة الموارد البشرية

> هذا المستند هو المرجع الرسمي السريع لتشغيل منصة **رابِط HR** محلياً وتنظيم العمل على المستودع.

## نظرة سريعة

- ⚙️ **الواجهة الأمامية:** React 18 + Vite + Tailwind CSS
- 🧠 **الواجهة الخلفية:** Express + tRPC + Drizzle ORM
- 🗄️ **التخزين:** MySQL / TiDB مع Drizzle ORM، وRedis اختياري للكاش والصفوف
- 🔐 **الأمان:** JWT، Security Headers، Rate Limiting، مراقبة سجلات (Winston)
- � **اللغات:** دعم متكامل للعربية والإنجليزية مع تبديل فوري

## المتطلبات المسبقة

| التقنية | الإصدار المقترح |
|---------|------------------|
| Node.js | 18 LTS أو أحدث |
| npm | 10 أو أحدث (مدعوم رسمياً) |
| MySQL / TiDB | 8.0 أو أحدث |
| Redis (اختياري) | للتخزين المؤقت والمعالجات غير المتزامنة |

> يمكن استعمال pnpm أو yarn، لكن `package-lock.json` يجعل **npm** الخيار الافتراضي المعتمد.

## البدء السريع

```bash
# 1) استنساخ المستودع
git clone https://github.com/<your-org>/RabitHR.git
cd RabitHR

# 2) تثبيت التبعيات
npm install

# 3) إعداد ملف البيئة
cp .env.example .env
# حرّر البيانات (MySQL, Redis, البريد, OAuth)

# 4) تهيئة قاعدة البيانات
npm run db:push

# 5) تشغيل الواجهة الأمامية والخلفية معاً
npm run dev:both
```

### السكربتات الشائعة

| المهمة | الأمر |
|--------|-------|
| واجهة Vite فقط | `npm run dev` |
| الخادم الخلفي فقط | `npm run dev:backend` |
| الواجهة + الخادم | `npm run dev:both` |
| بناء نسخة الإنتاج | `npm run build` |
| معاينة البناء | `npm run preview` |
| التحقق من الأنواع | `npm run type-check` |
| الاختبارات | `npm run test` |
| أوامر Drizzle | `npm run db:generate`, `npm run db:studio` |

## بنية المجلدات (مختصرة)

```text
├── server/                 # ملفات Express، المصادقة، خدمات البريد، السجلات
├── src/                   # واجهة React (مكونات، صفحات، hooks)
├── drizzle/               # مخططات الجداول وتعريفات Drizzle
├── scripts/               # مهام مساعدة (seed، patch، فحوصات)
├── docker-compose*.yml    # قوالب تشغيل عبر Docker
└── README_* + docs/       # بقية الأدلة المتخصصة
```

## خريطة الوثائق

| الملف | الوصف |
|-------|-------|
| `README_FINAL.md` | نظرة تسويقية وميزات تفصيلية |
| `README_COMPLETE.md` | دليل شامل للتقنيات والأمان والأداء |
| `README_DEPLOY_GUIDE_EN.md` / `README_DEPLOY_GUIDE_AR.md` | خطوات النشر (Vercel، Docker، الخوادم التقليدية) |
| `README_EMAIL_PROVIDER.md` | إعداد موفّر البريد (Resend / Nodemailer) |
| `README_REPORTS.md` | إنشاء التقارير ولوحات المتابعة |
| `README_GITHUB.md` | سياسات GitHub Actions وإدارة المستودع |
| أدلة أخرى: `DEPLOYMENT_GUIDE_FULL.md`, `SECURITY_*`, `ENV_*` | تفصيل الأمان والبيئة والإطلاق |

## تحقق قبل النشر

1. شغّل `npm run lint`, `npm run type-check`, و`npm run test` وتأكد من نجاحها.
2. تأكد من ضبط جميع متغيرات البيئة وإزالة أي أسرار من التحكم بالنسخ.
3. راجع قوائم `SECURITY_CHECKLIST.md` و`ENVIRONMENT_VARIABLES_FINAL_REPORT.md`.
4. حدّث الروابط والأصول (الشعارات، البريد، Sentry) قبل تنفيذ `npm run build`.

## الدعم والمساهمة

- مشاكل أو اقتراحات؟ افتح تذكرة GitHub أو تواصل عبر [support@rabit.sa](mailto:support@rabit.sa).
- للمساهمة راجع `DEVELOPER_GUIDE.md`, `CODE_DOCUMENTATION.md`, و`CONTRIBUTING` (إن وُجد).

**آخر تحديث:** 2025-11-25

> استخدم هذا المستند كنقطة دخول؛ بقية التفاصيل موزعة على ملفات README المتخصصة المذكورة أعلاه.
