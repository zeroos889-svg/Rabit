import type { AnyRouter } from "@trpc/server";

/**
 * Temporary placeholder for the backend AppRouter so the frontend can compile.
 * Replace this file with the real server router implementation once available.
 */
export type AppRouter = AnyRouter;

export const appRouter = null as unknown as AppRouter;
