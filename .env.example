# ===========================================
# مثال على متغيرات البيئة للمشروع
# Environment Variables Example
# ===========================================
# انسخ هذا الملف إلى .env وقم بتعديل القيم
# Copy this file to .env and modify the values

# ===========================================
# قاعدة البيانات - Database
# ===========================================
DATABASE_URL=postgresql://postgres:password@localhost:5432/rabit_db
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Note: تم التغيير من MySQL إلى PostgreSQL

# ===========================================
# Redis (اختياري - Optional)
# ===========================================
REDIS_URL=redis://localhost:6379
# If not available, app will continue without caching

# استخدام Redis لتحديد معدل الطلبات في بيئة موزعة
# Use Redis for rate limiting in distributed environment
USE_REDIS_RATE_LIMIT=false
# Options: true | false (requires REDIS_URL to be set)

# ===========================================
# OpenTelemetry Tracing (اختياري - Optional)
# ===========================================
OTEL_ENABLED=false
# Enable distributed tracing (default: false)

OTEL_SERVICE_NAME=rabit-hr
# Service name for traces

OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318/v1/traces
# OTLP exporter endpoint (Jaeger, Zipkin, etc.)
# Common endpoints:
# - Jaeger: http://localhost:4318/v1/traces
# - Zipkin: http://localhost:9411/api/v2/spans
# - Honeycomb: https://api.honeycomb.io/v1/traces
# - New Relic: https://otlp.nr-data.net/v1/traces

# ===========================================
# بيئة التشغيل - Environment
# ===========================================
NODE_ENV=development
# Options: development | production | test

# ===========================================
# المنافذ - Ports
# ===========================================
PORT=3000
# Backend server port

# ===========================================
# المصادقة - Authentication (TODO)
# ===========================================
JWT_SECRET=your-super-secret-jwt-key-change-in-production
SESSION_SECRET=your-super-secret-session-key-change-in-production

# ===========================================
# التطبيق - Application
# ===========================================
APP_URL=http://localhost:5173
# Frontend URL for CORS

VITE_APP_LOGO=/LOGO.svg
# Path to app logo (SVG recommended for best quality)

VITE_APP_TITLE=رابِط | Rabit
# Application title displayed in browser and meta tags

# ===========================================
# ملاحظات - Notes
# ===========================================
# - يجب تغيير الأسرار في بيئة الإنتاج
#   Change secrets in production!
# - لا ترفع ملف .env على Git
#   Never commit .env to Git!

# ===========================================
# Prometheus Metrics / مقاييس Prometheus
# ===========================================
METRICS_ENABLED=false
# Enable/disable Prometheus metrics collection
# تفعيل/تعطيل جمع مقاييس Prometheus

# Metrics are exposed at GET /metrics endpoint in Prometheus format
# المقاييس متاحة على نقطة النهاية GET /metrics بصيغة Prometheus

# Metrics include:
# - HTTP request count, duration, and status codes
# - Rate limiting metrics (blocked/allowed requests)
# - API version distribution
# - Error tracking by type and endpoint
# - Business metrics (payments, notifications, etc.)
# - Cache performance (when caching is enabled)
# - Database query performance
# - WebSocket connections (when WebSocket is enabled)
# - System metrics (CPU, memory, GC, etc.)

# ===========================================
# Cache Configuration / إعدادات الذاكرة المؤقتة
# ===========================================
CACHE_ENABLED=false
# Enable/disable caching (Redis-backed when REDIS_URL is configured)
# تفعيل/تعطيل الذاكرة المؤقتة (مدعومة بـ Redis عند توفر REDIS_URL)

CACHE_DEFAULT_TTL=300
# Default cache time-to-live in seconds (5 minutes)
# وقت البقاء الافتراضي للذاكرة المؤقتة بالثواني (5 دقائق)

# Cache Features / ميزات الذاكرة المؤقتة:
# - Automatic fallback to in-memory when Redis is unavailable
# - Get-or-set pattern with automatic caching
# - Pattern-based invalidation (e.g., "user:*")
# - Cache statistics and monitoring
# - Integration with Prometheus metrics
# - Batch operations (mget, mset)
# - Cache warming support
