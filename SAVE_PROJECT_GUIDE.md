# دليل حفظ المشروع محليًا
## Save Project Locally Guide

---

## 📥 طرق حفظ المشروع

### الطريقة الأولى: استخدام Git (موصى بها)

#### على جهازك المحلي:

```bash
# 1. استنساخ المشروع
git clone https://github.com/zeroos889-svg/Rabit.git

# 2. الانتقال إلى مجلد المشروع
cd Rabit

# 3. تثبيت الاعتماديات
npm install
```

---

### الطريقة الثانية: تنزيل كملف ZIP

1. اذهب إلى: https://github.com/zeroos889-svg/Rabit
2. اضغط على زر **"Code"** الأخضر
3. اختر **"Download ZIP"**
4. فك ضغط الملف في المكان المطلوب
5. افتح Terminal وانتقل للمجلد:
   ```bash
   cd path/to/Rabit
   npm install
   ```

---

### الطريقة الثالثة: نسخ من Dev Container

إذا كنت تعمل في Dev Container:

#### من VS Code:
1. افتح Explorer (`Ctrl+Shift+E`)
2. انقر بزر الماوس الأيمن على مجلد المشروع
3. اختر **"Download..."**
4. حدد المكان المطلوب لحفظ المشروع

#### أو من Terminal:
```bash
# إنشاء أرشيف مضغوط
tar -czf rabit-backup.tar.gz /workspaces/Rabit

# يمكنك تنزيله من خلال VS Code بعد ذلك
```

---

## 💾 حفظ التغييرات الحالية

### إذا قمت بتعديلات على المشروع:

```bash
# 1. إضافة جميع التغييرات
git add .

# 2. حفظ التغييرات مع رسالة توضيحية
git commit -m "وصف التغييرات التي قمت بها"

# 3. رفع التغييرات إلى GitHub
git push origin main
```

---

## 🔄 مزامنة التغييرات

### سحب آخر التحديثات:
```bash
git pull origin main
```

### حل التعارضات إن وجدت:
```bash
# حفظ تغييراتك المحلية مؤقتاً
git stash

# سحب التحديثات
git pull origin main

# استرجاع تغييراتك
git stash pop
```

---

## 📦 نسخة احتياطية كاملة

### إنشاء نسخة احتياطية شاملة:

```bash
# نسخة احتياطية مع التاريخ
git bundle create rabit-backup-$(date +%Y%m%d).bundle --all

# أو أرشيف كامل
tar -czf rabit-full-backup-$(date +%Y%m%d).tar.gz \
  --exclude=node_modules \
  --exclude=.git \
  /workspaces/Rabit
```

---

## 🎯 نصائح مهمة

### ✅ يُنصح بحفظها:
- جميع ملفات الكود (`client/`, `server/`)
- ملفات الإعداد (`.env`, `package.json`)
- الوثائق (`docs/`, `*.md`)
- إعدادات Docker (`docker-compose.yml`, `Dockerfile`)

### ❌ لا داعي لحفظها:
- مجلد `node_modules/` (يمكن إعادة تثبيته)
- ملفات البناء (`.next/`, `dist/`, `build/`)
- الملفات المؤقتة (`.cache/`, `coverage/`)

---

## 🔐 حماية البيانات الحساسة

⚠️ **مهم جداً:**

```bash
# تأكد من عدم رفع الملفات الحساسة
# الملفات التالية يجب أن تكون في .gitignore:
echo ".env" >> .gitignore
echo ".env.local" >> .gitignore
echo "*.key" >> .gitignore
echo "*.pem" >> .gitignore
```

---

## 📋 قائمة فحص النسخ الاحتياطي

- [ ] تم حفظ الكود المصدري
- [ ] تم حفظ ملفات الإعداد (بدون البيانات الحساسة)
- [ ] تم حفظ الوثائق
- [ ] تم حفظ إعدادات Docker
- [ ] تم التأكد من عدم حفظ `node_modules/`
- [ ] تم التأكد من عدم رفع `.env` لـ GitHub

---

## 🆘 استعادة المشروع

### من Git:
```bash
git clone https://github.com/zeroos889-svg/Rabit.git
cd Rabit
npm install
# نسخ ملف .env وتعديله
cp .env.example .env
```

### من أرشيف مضغوط:
```bash
# فك الضغط
tar -xzf rabit-backup.tar.gz
cd Rabit

# تثبيت الاعتماديات
npm install

# إعداد البيئة
cp .env.example .env
```

---

## 📞 معلومات إضافية

- **المستودع:** https://github.com/zeroos889-svg/Rabit
- **الفرع الرئيسي:** main
- **المالك:** zeroos889-svg

---

## 🎓 أوامر Git الأساسية

```bash
# معرفة حالة المشروع
git status

# معرفة التغييرات
git diff

# عرض سجل التغييرات
git log --oneline

# إنشاء فرع جديد
git checkout -b feature/new-feature

# التبديل بين الفروع
git checkout main

# دمج فرع
git merge feature/new-feature
```

---

**✨ تم إنشاء هذا الدليل في:** 5 ديسمبر 2025

**📝 ملاحظة:** احفظ هذا الملف كمرجع لك في المستقبل!
